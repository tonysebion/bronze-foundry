environment: dev
pattern_id: pattern5_hybrid_cdc_cumulative  # Unique identifier for tracing across source → bronze → silver
domain: retail_demo
system: retail_demo
entity: orders

path_structure:
  sample_key: "sample"
  system_key: "system"
  entity_key: "table"
  date_key: "dt"
  silver_model_key: "silver_model"
  domain_key: "domain"
  load_date_key: "load_date"

bronze:
  enabled: true
  source_type: file
  path_pattern: ./sampledata/source_samples/sample=pattern5_hybrid_cdc_cumulative/system=retail_demo/table=orders/dt=2025-11-24/delta/delta-part-0001.csv
  partition_column: run_date
  options:
    format: csv
    load_pattern: cdc
    pattern_folder: pattern5_hybrid_cdc_cumulative
    delta_mode: cumulative
    match_dir: delta

silver:
  enabled: true
  entity_kind: derived_state
  history_mode: scd2
  delete_mode: ignore
  schema_mode: allow_new_columns
  natural_keys:
    - order_id
  change_ts_column: changed_at
  event_ts_column: changed_at
  attributes:
    - status
    - delta_tag
  partition_by:
    - event_ts_dt
  # V1 Unified temporal configuration: partition by record time (state change time), not load time
  record_time_column: "changed_at"  # Column with state change time
  record_time_partition: "effective_from_date"  # Partition key name for state change date
  load_batch_id_column: "load_batch_id"  # Standard column for batch tracking
