# Database extractor support (SQL Server, PostgreSQL, MySQL, Oracle, MongoDB)
# Install with: pip install -r requirements.txt -r requirements-db.txt
# Or use: pip install -e .[db]

# ODBC driver (required for SQL Server via ODBC)
pyodbc>=4.0.0

# SQL Server native driver (alternative to ODBC)
pymssql>=2.2.0

# PostgreSQL
psycopg2-binary>=2.9.0

# MySQL
mysql-connector-python>=8.0.0

# Optional drivers (uncomment as needed):
# Oracle
# cx_Oracle>=8.3.0

# MongoDB
# pymongo>=4.0.0
